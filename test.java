import java.time.*;
public class test {
    public static void main(String[] args){

       int posX = (int) (Math.random()*100);
        int posY = (int) (Math.random()*100);

        System.out.println("Ball has been created with default values at position: X: " + posX +" Y: " + posY);
    }
    
}
